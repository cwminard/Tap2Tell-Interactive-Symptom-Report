<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tap2Tell"
        tools:targetApi="31">

        <!-- LOGIN (Launcher) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- DASHBOARD -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />

        <!-- SIGNUP -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false" />

        <!-- LOGOUT SCREEN -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- CHAT SCREEN -->
        <activity
            android:name=".chat"
            android:exported="false" />

        <!-- SYMPTOMS SCREEN -->
        <activity
            android:name=".Symptoms"
            android:exported="false" />
        <activity android:name=".AdminApprovalActivity" />
        <activity android:name="com.example.tap2tell.providerinfo.userdetails" />
        <activity android:name=".providerinfo.Providersignin" />
        <activity android:name="com.example.tap2tell.providerinfo.providerdashboard" />
        <activity android:name=".providerinfo.providerchat" />
        <activity android:name=".Aiactivity" />






    </application>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
